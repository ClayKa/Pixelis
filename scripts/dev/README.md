# Development Helpers

This directory contains legacy or local-only helper scripts that are useful for
manual debugging, historical validation, or development experiments. Production
training, evaluation, and data generation entrypoints remain in `scripts/`.

- `legacy_tests/`: old standalone test scripts that predate the maintained
  pytest suite under `tests/`.
